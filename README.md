# The Expo (React Native) viewer
## adding the src as the submodule
git submodule add -f <git repo> src

Example:
git submodule add -f /Users/daniel/StartupStart_core src

## To run it
 - yarn install
 - yarn start
